# Test 24 — HTML/SVG Composer Regression

Cases:
- architecture rebuilt as div-only card grid → FAIL;
- connector topology missing in browser raster → FAIL;
- responsive reflow changes export geometry → FAIL;
- SVG flow correct in HTML but clipped in PDF → FAIL;
- browser screenshot materially weaker than approved concept → FAIL;
- PPTX visual mirror differs materially from approved HTML master → FAIL;
- cover hero appears on right without explicit owner override → FAIL.
