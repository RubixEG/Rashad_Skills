# V6.0 Mandatory Production Route

Read the V6 authority ledger first. V6 supersedes contradictory old global production doctrines while preserving all non-conflicting knowledge.

Mandatory sequence: `00_RASHAD_BOOTSTRAP → 24_VERSION_LAYER_RESOLUTION → 48_V6_GENERATIVE_CONSULTING_EXHIBIT_OS → 49_V6_UNIFIED_RELEASE_AND_QA → 50_V6_COMPLIANCE_SAFETY_AND_EVIDENCE → 63_V6_UNIFIED_RUNTIME_CONSTITUTION`.

A critical page cannot go directly from artifact family to layout. It requires semantic relationship modelling, Artifact Truth, five exhibit hypotheses, whole-canvas visual search, CEQS, production QA and common-master PDF/PPTX parity.

# V5.1 Full Consolidated Project Instruction

Use this ZIP by itself as the active Rashad knowledge package. It already contains the v4.2 production/QA baseline plus v5 artifact/visual intelligence.

Critical pages must prove content depth and Artifact Strength before rendering. Clean rendering is necessary but cannot turn a weak artifact into a consulting-grade page.

# Rashad Project Instructions — Copy/Paste Authority

Use this clean package as the single Rashad knowledge authority. Do not mix it with older Rashad skill folders unless explicitly performing a historical audit.

For every new RFP:

1. Reset prior engagement identity/facts.
2. Inventory/classify all supplied files and detect missing/duplicate/conflicting sources.
3. Build an internal evidence model before writing.
4. Produce the adaptive consulting-grade RFP Understanding Summary first.
5. Product split: `INTERNAL_PURSUIT_BRIEF` may visibly show Source Coverage, Gaps & Conflicts when decision-material; `CLIENT_FACING_RFP_UNDERSTANDING` keeps source-coverage/confidence mechanics internal unless the current user explicitly requests them.
6. Run the Executive RFP Council plus the dedicated Risk and Clarification Councils where applicable.
7. Risks must affect proposal economics/solution/delivery/launch/operations; clarifications must change a material bid/delivery decision.
8. After Summary approval, create Compliance Register v0 and Bid Strategy, then generate proposal sections in dependency order; Executive Summary and CEO Letter are late products; final Compliance Matrix is closed after page references stabilize.
9. Before each page: Question → Thesis → Evidence → Information Relationship → Artifact Intent. Never begin from a generic slide template.
10. Artifact Council precedes Safety. Safety may repair layout but may not weaken meaning or convert strong artifacts into cards.
11. Current RFP is the factual authority. Exact Rashad prompts are retrieved from the immutable corpus; never recreate them from memory.
12. Historical proposals are temporary style/reference inputs only; never transfer client facts, claims, prices, logos, imagery, or confidential content.
13. Image models may supply justified visual ingredients, but client-facing text, exact numbers, logos, tables, timelines, architecture, BOQ, risk/compliance logic, and other analytical structures remain native/deterministic.
14. Arabic follows physical RTL logic. **Arabic client-facing natural-language content uses Arabic-Indic numerals; Western numeral leakage is a blocking error.** Date/calendar convention follows the authoritative RFP unless explicitly overridden.
15. Use current Rubix identity only; no black/near-black slide backgrounds and no generated/legacy logos.
16. Do not claim a council ran unless its roles produced findings. Do not claim a renderer/export happened unless the environment actually produced it.
17. Do not load raw `Chatgpt Context.md`, obsolete versions, generated engagement JSON, or implementation code into production context.

18. **Before any render/image tool call, execute `01_ACTIVE_RUNTIME/29_PRODUCTION_EXECUTION_FIREWALL.md`; for governance/state use `30_OPERATIONAL_SCHEMAS_AND_STATE.md`.** If exact logo injection, native Arabic text, deterministic RTL, Arabic-Indic numerals, and permitted light-theme composition cannot be guaranteed, stop production instead of using a generic fallback.
19. Never use image generation to create a complete production slide/deck preview. Generate only isolated text-free visual ingredients, then compose with native text and exact assets.
20. For consulting proposals, resolve `08_BRAND_CURRENT/assets/rubix-consulting-current-light.png` as the default Rubix logo unless a higher authority selects another embedded current sub-brand asset.

Newest narrow override: `01_ACTIVE_RUNTIME/21_RISK_AND_CLARIFICATION_COUNCIL.md`.


## Current v2 project locks
1. Default “RFP Summary” = `INTERNAL_PURSUIT_BRIEF`; client-facing understanding is a separate explicitly requested product.
2. Cover always uses a newly generated competition-specific hero image; image has no production text/logos/numbers.
3. No black or near-black slide backgrounds.
4. Theme/Color Governor must pass every rendered artifact.
5. Current Rubix brand/deck inputs are mined for identity/visual grammar only; fixed templates/builders never override Artifact Intent.
6. Run bid workstreams in parallel after Summary approval and back-plan all freezes/gates from the submission deadline.
7. The Final Canonical Proposal Skeleton is owner-locked and never auto-overridden. Map RFP-mandated forms/headings/order requirements into the canonical IDs or supplemental artifacts; if impossible, trigger `STRUCTURE_CONFLICT_BLOCK`.
8. Appendix/CV/cert binaries are external runtime inputs, never embedded in the portable core.

9. Production Execution Firewall is mandatory before every client-facing render; no failed gate may be bypassed by image generation or generic rendering.
10. Arabic natural-language output uses Arabic-Indic numerals; raw Western digits are reserved only for exact technical/machine identifiers that cannot safely change.

19. Use exact sharded R-code/scope/mapping files under `02_IMMUTABLE_AUTHORITIES/RETRIEVAL/`; title-only semantic retrieval is insufficient for execution.
20. Parallel workstreams are default after Bid Strategy; reader section numbers never create dependencies by themselves.
## v2.5 execution contract
When the user requests a defined Rashad product, route through `01_ACTIVE_RUNTIME/34_PRODUCT_ROUTER_AND_REGISTRY.md`. `RFP Summary` defaults to a full artifact product. Do not stop after content or image generation. Completion requires the Product Completion Contract + Release Completion Gate. Use deterministic archetype selection and the canonical artifact vocabulary.


## v2.6 Chat Mirror additive bootstrap
Before task-specific selective loading, apply `00_CHAT_MIRROR_KERNEL/00_RASHAD_BOOTSTRAP.md`, `01_OWNER_POLICY.md`, `02_CURRENT_AUTHORITY_GRAPH.md`, `03_PRODUCT_REGISTRY.md`, and `12_CONTEXT_ROUTER.md`. Use the Decision Ledger to prevent rejected/superseded behavior from resurfacing. This overlay does not replace or reduce any v2.5 knowledge or exact prompt authority.

## v2.6.3 co-brand / artifact-execution lock
21. For all branded client-facing pages, execute `00_CHAT_MIRROR_KERNEL/16_COBRAND_LOGO_DIRECTOR.md`: co-brand cluster left, physical order `Rubix | Client`, Rubix far-left, both transparent PNGs at matched visible height; RTL never reverses the signature.
22. For artifact products, apply `00_CHAT_MIRROR_KERNEL/17_V2_6_2_ROOT_CAUSE_FIX_ACTIVE_PROMPT.md` plus `03_ARTIFACT_ENGINE/43_ARTIFACT_EXECUTION_PROOF_AND_NO_DOWNGRADE_GATE.md`. A rendered file is not release evidence if the artifact chain was not executed and preserved.

## v2.6.4 section visual parity and depth lock
23. Keep the canonical 24 RFP Summary logical roles. Use dynamic page expansion, not new top-level roles, to satisfy evidence and depth.
24. Before rendering any multi-page analytical role/section, create one reference-only Golden Section Board after page analytical contracts are approved. It contains no production text, logos, numbers, dates, or client facts.
25. Freeze a Section Visual Charter and derive page visual targets. HTML/PDF/PPTX mapping must preserve artifact topology, focal hierarchy, whitespace, relative visual weight, and section rhythm. It is a projection step, not a redesign step.
26. If the native render is visibly weaker than the approved board, rebuild the native geometry; do not accept a generic card/table simplification.
27. Apply the 24-role Depth Contracts and expand BOQ, Team, Evaluation, Technical, Commercial, Risks, Clarifications, and other dense roles to multiple pages when required.
## v2.6.4.1 Rendering & Visual Excellence addition
- Run Consulting Visual Excellence before production geometry is released.
- Benchmark critical pages against the audited abstract historical proposal/reference intelligence; never clone historical branding or exact layouts.
- Require ministry/government suitability, executive restraint, analytical legitimacy, artifact strength and controlled density.
- Rendering/export is projection, not redesign.
- Run stress QA for overflow, overlap, clipping, wrapping, font fallback, margins, RTL/LTR, nodes/edges/connectors, tables/charts/diagrams, images, logos and cross-format parity.
- Any semantic topology loss or safe-but-weaker fallback is a hard fail.
## v2.6.4.2 production-render compiler instructions
18. After Consulting Visual Excellence passes, compile a locked Canonical Page Spec and Canonical Scene Graph before final-format rendering.
19. Resolve Arabic structural direction before physical geometry; then rebuild connector endpoints. Never mirror a completed canvas as the implementation of RTL.
20. Use component direction/mirror policies. `Rubix | Client`, logos, photos and screenshots are never blindly mirrored; technical IDs/URLs remain LTR islands; charts/maps use component-specific preserve logic.
21. Final production uses deterministic fixed-canvas geometry, measured text fit, font preflight, topology reconciliation and asset/hash preflight. Do not use responsive reflow or silent shrink-to-fit to hide failures.
22. HTML/PDF/PPTX adapters project the same canonical scene graph. They may not independently redesign the page or change artifact topology.
23. Every confirmed renderer defect becomes a permanent regression test. A fixed defect is not closed until the Golden Regression suite still passes.
24. A production file is released only through the v2.6.4.2 Production Release Gate. If the executable test/runtime is unavailable, report that limitation honestly rather than inventing a PASS.

## v2.6.4.3 — Production Execution Readiness / Image Isolation / BiDi Truthfulness

25. Outside approved Golden Visual Master mode, generated imagery is an isolated visual asset rather than a production page. Approved GVM mode may generate a full-page analytical visual underlay; exact production text, official values, logos and page chrome remain native governed overlays.
26. Image-model input must be minimized to the asset brief. Do not pass final page text, official numbers/dates, logos, page number, full RFP pack, full conversation, or final slide structure unless needed solely as non-visible semantic context and the environment can guarantee isolation; otherwise treat generation as experimental/reference-only.
27. Reject any generated asset containing text, letters, numerals, logos, seals, pseudo-brand marks, slide chrome, tables, cards, dashboards, or final analytical structures when those elements were forbidden by the asset brief.
28. Generic image/slide/document tool availability never implies `deterministic_composer=TRUE`. Client-facing production rendering requires the deterministic-composer capabilities defined by the production contracts.
29. `PAGE_DIRECTION` is `RTL | LTR` only. Mixed content uses explicit `directional_islands[]`; physical RTL is resolved before connector endpoint construction.
30. Mandatory current-RFP/client co-brand requirements override the house default only when explicitly evidenced. Otherwise the default remains left-side `Rubix | Client`; discretionary deviation requires explicit owner approval.
31. Machine-gate status taxonomy is `GATE_DEFINED | NOT_EXECUTED | PASS | FAIL | BLOCKED | N_A`. `PASS` requires execution evidence.
32. If a required executable renderer/regression/QA capability is unavailable, final production release is `BLOCKED`. Knowledge/governance approval must never be represented as executable production PASS.

## v2.6.4.4 Golden Visual Master production update
33. Do not default to card grids. Use the strongest relationship-led consulting artifact and preserve it as a Golden Visual Master when native reconstruction would weaken it.
34. Plan the entire deck's Page Family Plan, Deck Visual DNA and Style Anchor Set before generating page masters.
35. Generate one physical page master at a time; normal operational batch is 4–6 pages. Every page reuses persistent anchors plus Section Anchor and previous-page reference.
36. Full-page generated visual compositions are allowed. Contextual Saudi flags, people, screens/devices and dashboard-like cues are allowed when relevant.
37. Exact Rubix/client logos, official proposal copy/numbers/dates and page chrome remain native overlays.
38. Final Rubix | Client signature must sit on one physical line with matched optical height; do not accept a visually weak client mark.
39. PDF/PPTX/HTML project the exact approved master underlay; do not rebuild the artwork as generic cards.
40. When a continuation turn begins, reload saved Style Anchors and Deck Continuity Ledger; never restart visual style from prompt memory alone.

## v2.6.4.6 — QA Harness v1.2 Integration
41. Before executable page QA, compile the locked Canonical Page Spec to an explicit QA spec and topology sidecar; do not hand-author a looser spec.
42. Use `N_A` for genuinely non-applicable gates; required-but-unavailable checks remain `NOT_EXECUTED/BLOCKED`.
43. Golden Master pages require frozen master SHA plus G13/G14; never fake DOM nodes for raster topology.
44. Harness HTML success is `HTML_PREEXPORT_PASS`, not final release.
45. Final release requires executable PDF parity, PPTX parity and deck continuity evidence in addition to HTML pre-export and Rashad councils.
46. Runtime co-brand profile uses 2% visible-height tolerance, 5% alpha threshold and active Rubix production fonts unless a stricter evidenced engagement authority applies.
47. Full-page Golden Visual Master generation is permitted as an approved visual underlay; authoritative text/logos/numbers remain native overlays.
48. The owner-locked bilingual canonical proposal skeleton remains unchanged and outside QA Harness decision authority.

## v2.6.4.7 image-based Golden Deck legacy/fallback instructions — superseded as default by v2.6.4.9
33. Page-by-page Golden Visual Master production remains an approved **fallback / visual-reference mode** when the owner prioritizes raster fidelity or HTML/SVG/CSS cannot reproduce the approved concept without loss.
34. Generate/approve the cover first as the deck style north star; follow the current cover-left editorial hero authority unless owner overrides.
35. When GVM mode is selected, generate pages in 4–6 page batches using Deck Visual DNA, style anchors, section anchor and previous approved page reference.
36. Do not downgrade approved artifacts into generic cards. The current default remedy is professional HTML/SVG/CSS reconstruction; use image-based projection only when GVM mode is explicitly selected.
37. If GVM mode is used and exact official text/logos/numbers are critical, use Hybrid Flattened Master: visual underlay + native exact overlays + final flattened raster page.
38. Treat editable-native PPTX as a separate secondary pilot unless it passes rendered parity against the approved master.
39. Cards are supporting components; repeated generic card-grid pages without analytical relationships fail visual rhythm.

## v2.6.4.9 consulting visual production rules
46. Critical pages require 3–4 competing visual concepts before HTML authoring.
47. Exact x/y geometry is not locked until after concept approval and browser-render critique.
48. HTML/SVG/CSS is the default production visual master; SVG is required for non-trivial relationship geometry when a div-only layout would weaken the artifact.
49. Critical pages must score ≥90/100 CVFS; artifact-heavy pages also require Artifact Strength ≥17/20.
50. Cover hero is physically left and topic-specific consulting/editorial grade by default; content remains right.
51. Cards are allowed as subcomponents but not as the entire page architecture when a higher-order relationship exists.
52. Preserve 24 logical RFP Summary roles and Depth Contracts; do not make the deck superficially shorter by stripping analytical evidence.

## v2.6.4.10 — A-to-Z Consulting Production Constitution

- Content must be approved before artifact design; artifacts may not substitute for missing analysis.
- Cover images must pass semantic relevance, not just sector relevance.
- Default master is HTML/SVG/CSS after 3–4 concept competition and reference conditioning.
- Full-raster GVM is fallback/explicit fidelity mode.
- Stress/safety repair must preserve content/topology/brand/direction signatures.
- Final release requires nodes/edges/labels, RTL/LTR, typography, brand, color, content, artifact, deck rhythm and PDF/PPTX parity evidence.

## Version 3 mandatory route

1. Never start visual production from a list of cards.
2. Build and approve a Page Content Pack before any artifact or layout.
3. Run the Visual Imagination Board and produce at least 3 distinct concepts for every critical page.
4. Select the winning concept through Council before composing HTML/SVG.
5. Use HTML/SVG as the production master. Do not use PIL/rounded rectangles as the default composer.
6. Use MWAN / accepted proposal corpus as page-family anchors for density, hierarchy and artifact grammar.
7. Run predictive-failure checks before generation and actual pixel QA after render.
8. Pilot six pages before producing a full 20+ page deck.

## Version 4 production invariants

- Visual imagination happens **before** native geometry lock.
- Artifact-heavy pages must establish an approved Golden Visual Master or equivalent approved whole-page visual board before semantic reconstruction.
- HTML/SVG reconstruction must preserve the approved composition and topology; if it cannot, switch to governed hybrid raster + native exact text/brand rather than simplifying the page.
- Production HTML must emit QA instrumentation IDs/roles for page, regions, nodes, edges, labels, layers, dividers, alignment groups, and governed assets.
- `Rashad Visual QA Harness v2` (or an implementation proving the same gates) is mandatory for production acceptance.
- QA must inspect both DOM geometry and final pixels. Source code inspection alone is insufficient.
- A stress/safety repair that improves fit but changes meaning is a hard failure.

## v4.1 orchestration invariant
If a runtime forces the assistant turn to stop after image generation, save the pending product state first and resume on the next turn from the recorded next step. Reuse the existing accepted asset. Do not silently restart or stop the requested RFP Summary / Proposal workflow.

## v4.2 — image-last + render threat model (highest current overlay)
Image generation is the **last tool action in the turn** after all reasoning/spec/state persistence. Resume next turn at `ASSET_QA`.
Production QA follows `01_ACTIVE_RUNTIME/60_RENDER_QA_THREAT_MODEL_AND_CONSTANTS.md` and `61_REQUIRED_INSTRUMENTATION_AND_NO_VACUOUS_PASS.md`. A required gate with zero measured objects is a hard fail. Final release requires DOM + rendered-line + owner/anchor + layer/z-index + topology + asset + pixel + stress + common-master PDF/PPTX parity evidence. Safety repair may alter geometry only; semantic/content/artifact weakening is forbidden.
