# Owner / Anchor / Badge / KPI Containment Engine

Every dependent micro-element declares `data-owner-id` and `data-anchor`. Applies to sequence badges, multi-digit numbers, KPI values, icons, phase markers, status chips, footnote markers and arrow labels. QA validates anchor-zone containment after final font/size resolution. 1→2→3→4 digit growth is explicitly stress-tested.
