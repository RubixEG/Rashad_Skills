# V6.2 Reference, Asset & Knowledge Readiness Lock

**STATUS: CURRENT TRUTHFULNESS AUTHORITY FOR REFERENCE/ASSET CLAIMS**

1. Historical proposals and generated “last version” pages are reference grammar only.
2. Shipped current Rubix PNG assets are authoritative only after hash/inventory verification.
3. No font binaries are claimed as bundled. Exact brand typography requires externally supplied/licensed font assets verified at runtime.
4. No SVG icon binaries are claimed as bundled. The SVG pipeline is a production standard; the actual icon inventory controls availability.
5. External appendix/company evidence (CVs, case studies, certificates, registrations, client assets) is a runtime dependency. Missing evidence must be reported and may block affected proposal claims/pages.
6. Machine-readable brand tokens in `08_BRAND_CURRENT/tokens.json` / `tokens.css` mirror the Markdown palette authority and do not authorize using every color on every page.
7. Historical numeral styling never overrides the current Arabic numeral authority.
