# Rendered Line Fragment & Glyph Collision QA

Use `Range.getClientRects()` for visible text runs. Check line-to-line and independent-element intersections, clipped ascenders/descenders, Arabic shaping, punctuation, LTR islands, replacement glyphs and unexpected wrapping. Container fit does not prove text fit.
