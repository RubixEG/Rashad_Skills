> **V6.2.2 SUPERSESSION BANNER — Legacy visual-search authority detail. The 3–4 concept count is superseded by exactly 5 critical-page hypotheses; HTML/SVG is not a universal fidelity route. Preserve only non-conflicting functional-emulation principles.**

# Visual Reasoning Functional Emulation Contract

STATUS: LEGACY VISUAL-SEARCH SPECIALIST DETAIL — GLOBAL AUTHORITY REVOKED

Rashad cannot access or copy the private hidden chain-of-thought of an image-generation model. Instead, Rashad must reproduce the **observable useful behavior** that creates strong full-page compositions.

## Functional emulation loop
1. read the whole page content as one system;
2. identify one dominant visual idea;
3. create a full-canvas mass/whitespace/eye-path plan;
4. LEGACY lineage formerly generated 3–4 concepts; CURRENT critical-page law generates exactly 5 materially distinct hypotheses;
5. benchmark them against current-deck / MWAN / proposal-corpus grammar;
6. choose through council scoring, not coding convenience;
7. build a low-fidelity HTML/SVG candidate;
8. render to pixels;
9. critique the entire raster globally;
10. repair macro composition before micro details;
11. repeat until thresholds pass.

This is the required functional mirror of image-generation-style composition thinking.
