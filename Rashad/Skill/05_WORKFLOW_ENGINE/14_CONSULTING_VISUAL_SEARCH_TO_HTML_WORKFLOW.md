> **V6.2.2 SUPERSESSION BANNER — Legacy visual-search workflow; 3–4 candidate count and universal HTML implication are superseded. Current critical pages require exactly 5 hypotheses and ≥3 actual rendered search candidates; production mode follows current fidelity authority.**
# Consulting Visual Search → HTML/SVG/CSS Workflow

`RFP evidence → role/page contract → management question → thesis/evidence → information relationship → Visual Thought Board → 3–4 concept candidates → Visual Council selection → reference anchor pack → relative composition blueprint → HTML/SVG/CSS candidate → browser raster → critique/repair → geometry freeze → QA → PDF/PPTX projection`.

## Critical rule
The renderer is not allowed to choose the visual concept. Concept choice happens upstream through visual reasoning and council selection.
