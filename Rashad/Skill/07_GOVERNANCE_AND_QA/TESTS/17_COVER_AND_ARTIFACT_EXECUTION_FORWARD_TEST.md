# 17 — Cover & Artifact Execution Forward Test

STATUS: BLOCKING BEHAVIOR TEST

For an Arabic RFP Summary forward-test:

1. A new engagement-specific hero image is actually generated.
2. The hero contains zero generated text, logos, numbers, dates, tender IDs, or client names.
3. Arabic cover composition is hero-left / native identity-right unless explicit owner override exists.
4. An abstract vector/network placeholder cannot satisfy the hero requirement.
5. Every relationship-heavy analytical page has the complete Artifact Execution Trace defined in `43_ARTIFACT_EXECUTION_PROOF_AND_NO_DOWNGRADE_GATE.md`.
6. `SCOPE_ARCHITECTURE`, `DELIVERY_JOURNEY`, `BOQ_INTELLIGENCE`, `EVALUATION_WIN`, `COMMERCIAL_EXPOSURE`, `RISKS`, `CLARIFICATIONS`, `AUTHORSHIP_MATURITY`, and `BID_DECISION` cannot release as generic equal-card grids.
7. Artifact strength is scored before fit/safety repair. Required score: >=7/10.
8. HTML and PDF raster must preserve artifact family, node/edge topology, focal point, crop, logos, and RTL geometry.
9. If HTML/PDF parity fails, release fails. Do not claim the PDF is validated.
10. If deterministic composition cannot preserve the approved artifact, report production BLOCKED rather than substitute a weak generic layout.
