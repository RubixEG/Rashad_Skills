# Header Stack & Reflow Engine

Instrument `EYEBROW`, `TITLE`, `SUBTITLE`, `ACCENT`, `ARTIFACT_START`. Header uses measured flow, not fixed assumptions. Two/three-line titles push everything below them. Measure post-font-load line fragments and enforce minimum vertical gaps. Independent title/subtitle fragments may never intersect.
