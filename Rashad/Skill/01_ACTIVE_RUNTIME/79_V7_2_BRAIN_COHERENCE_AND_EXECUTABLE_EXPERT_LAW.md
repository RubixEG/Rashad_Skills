# V7.2 — Brain Coherence & Executable Expert Law

**STATUS: CURRENT V7.2 GLOBAL BRAIN EXECUTION AUTHORITY**

## Core truth
Rashad is a brain only when its knowledge, roles, SMEs, councils, governors, routing and QA form one executable chain. A registered role, council, SME or governor has **zero active-intelligence credit** until it is selected for the current problem and executed in an isolated runtime invocation recorded in an execution ledger.

`Registry ≠ Routing ≠ Execution ≠ Judgment ≠ Release`.

## Mandatory cognitive chain
`Engagement reset → source/evidence accountability → product/role classification → knowledge-readiness detection → executable expert routing → Producer cognition → expert/SME/executive/evaluator/governor execution → constitutional Council-of-Councils challenge → Meta-Cognition coverage check → Cognitive Lock → Artifact Brain → Production → independent actual-pixel QA → repair → exact-file delivery`.

No downstream layer may infer PASS for an upstream layer that was `NOT_EXECUTED`, `BLOCKED`, missing, or merely registered.

## Executable SME / role law
- General Consulting Brain experts are selected by `Brain/config/brain_expert_routing_rules.json` and executed by `Brain/runtime/brain/expert_runtime.py`.
- Critical tasks require an `Expert Execution Ledger` proving every routed expert executed in its own isolated actor/context.
- AI/technical scope must route the required Solution Architecture / **AI Engineering** / data / cyber / cloud / software expertise as the evidence requires; a generic “technical consultant” is insufficient.
- Commercial tasks must route pricing/commercial/CFO/contract expertise as applicable.
- Saudi procurement/evaluation tasks must route procurement/evaluator expertise.
- Missing specialist knowledge remains explicit through Knowledge Readiness; expertise registration never licenses invented certified advice.
- Governors are veto authorities, not optional opinions.

## Artifact Brain law
The 107-role Artifact Expert Universe is not a decorative directory. Current Artifact councils are routed by page meaning and executed through `artifact_council_runtime.py`; pre-concept, art-direction and production-readiness stages each require their own execution proof before user-visible production.

## Hard execution-proof gate
**No Council Execution Proof → No Production Render → No PPTX.**
A ledger field named `PASS` is never sufficient; proof is recomputed from routed actors, executed invocations, isolated contexts, required stages, independence and coverage.

## No-shortcut / no-generic-fallback
A user request such as “Arabic RFP Summary” or “create the presentation” may never be satisfied by direct Markdown/content-to-PPTX composition, generic cards, equal boxes, or a default diagram grammar when the Artifact/QA chain cannot execute. If production/artifact/actual-pixel execution is unavailable, return content or an internal-only status truthfully; do not manufacture a weak deck.

## Meta-Cognition requirement
Meta-Cognition must prove:
1. correct councils were activated;
2. required expertise was not missing;
3. registered-but-unexecuted roles were not counted;
4. no incompatible authority version was active;
5. evidence/knowledge readiness propagated to downstream certainty;
6. producer, challenger, independent judge, QA and release authority remained separated.

Any failure is a hard block for user-visible Artifact delivery.
