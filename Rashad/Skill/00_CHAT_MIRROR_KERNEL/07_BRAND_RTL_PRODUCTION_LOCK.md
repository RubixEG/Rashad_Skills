# Brand / Arabic RTL / Production Lock — Mirror Summary

STATUS: ACTIVE ALWAYS-ON HARD GATES
CANONICAL AUTHORITIES: v2.5 brand, Arabic/RTL, firewall and production modules

## Brand
- Current verified Rubix asset only.
- Preserve aspect ratio; no crop, mirror, recolor or generated/reconstructed logo.
- Client co-branding uses the approved client asset; missing asset must never be fabricated.
- Historical proposal brand assets are prohibited.

## Background
Black and near-black client-facing slide backgrounds are prohibited under current v2.5 policy.

## Arabic
- Native Arabic drafting, not literal English translation.
- Arabic visible structural labels remain Arabic.
- Physical reading order is RTL, not merely right-aligned text.
- Timelines/sequences begin visually from the right when the Arabic product requires it.
- Mixed technical tokens remain correctly isolated LTR.
- Current Arabic-Indic numeral rule applies to natural-language client-facing content.

## Cover
Arabic RFP Summary cover uses left hero / right native identity-text zone unless explicitly overridden by the owner.

## Production
No full-slide image generation. Exact text/logos/numbers and analytical structures are composed natively by the permitted deterministic production path. If the capability does not exist, fail closed at production rather than silently changing the product.

## Co-brand geometry
- Always execute `16_COBRAND_LOGO_DIRECTOR.md` for branded client-facing artifacts.
- Physical cluster location: left.
- Physical order: `Rubix | Client`; Rubix remains far-left even in Arabic/RTL products.
- Both logos are transparent-background PNG assets and are normalized to the same visible/optical height.
## v2.6.4.2 structural direction extension
- RTL is resolved as physical geometry, not as `text-align:right` or a whole-page CSS flip.
- Use logical START/END and component-level direction/mirror policies before physical coordinates are finalized.
- Ordered Arabic timelines/processes/sequences normally place semantic item 1 physically on the right.
- Resolve direction first, then node positions, then rebuild connector endpoints.
- `Rubix | Client`, logos, photographs, screenshots and other no-mirror assets follow the explicit registry in `03_ARTIFACT_ENGINE/64_RTL_STRUCTURAL_QA_AND_NO_MIRROR_REGISTRY.md`.
- An unjustified LTR structural flow on an Arabic ordered artifact is a hard release blocker.

## v2.6.4.3 co-brand precedence
Mandatory current-RFP/client brand requirements may override the house signature only when explicitly evidenced. Otherwise `Rubix | Client` remains physically left; discretionary changes require explicit owner approval.
