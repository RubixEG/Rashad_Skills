---
name: rashad-proposal-os
description: "Rubix Consulting RFP/proposal operating system with evidence-grounded consulting intelligence, canonical RFP decision architecture, generative exhibit synthesis, Arabic/RTL governance, conditional executive/government councils, total visual QA and adversarial release contracts."
---

# Rashad Proposal OS v7.3.0 — Visual Production Organ, Composition Intelligence & Council-Supervised Engagement Acceptance

**STATUS: CURRENT / HIGHEST SKILL RELEASE — V7.3 CERTIFICATION CANDIDATE**  
**BASELINE: v7.0.2 consulting/decision architecture preserved; v6.2.2 protected corpus remains inherited without deletion.**  
**BOUNDARY: Skill authorities are certified here. Rashad OS runtime truth is separate: `Rashad/Brain` and `QA/Runtime + QA/Brain` are certified foundations. Cognition may execute through `HOST_NATIVE_MODE` (ChatGPT/Claude/approved host; no external API key), `API_PROVIDER_MODE`, or `OFFLINE_VALIDATION_MODE`. Actual invocation, rendered-output QA and release evidence must still prove execution at runtime.**

## Mandatory startup
Execute `00_CHAT_MIRROR_KERNEL/00_RASHAD_BOOTSTRAP.md`. Root `ACTIVE_AUTHORITY_MANIFEST.json` is the sole machine global-routing source of truth.

## Consulting intelligence
Critical analytical content is not produced from a section name alone. Compile:
`Management Question → Evaluator/Assurance Question → Decision Supported → Evidence For/Against → Answer-First Thesis → Counterarguments/Assumptions → Semantic Relationships → Executive Implication`.

Use the conditional Council-of-Councils router. V7 Council IDs are analytical **lenses**, not invented runtime role identities; every lens resolves through `council_lens_registry_v7.json` to one or more of the 29 authorized runtime roles. Producer, Challenger, Independent Judge and Release Chair are separate functions. Council reasoning is not visible filler.

## RFP Summary canonical lock
The existing **24 logical roles and order remain canonical**; physical page count is dynamic. `rfp_summary_role_registry_v7.json` owns canonical IDs, aliases, Arabic/English visible names, required analysis and evidence expectations. Do not invent or cosmetically rename sections.

Arabic selected output → Arabic visible product. English selected output → English visible product. Foreign-language islands are limited to official/proper/technical tokens where translation would reduce precision. Decorative bilingual headings are forbidden.

Role 23 keeps the evidence-based authorship/procurement maturity fingerprint, including whether the package appears limited/single-internal-author, multi-function internal, specialist advisory-led, hybrid/compiled, or insufficient evidence. Never identify a named advisor/person without explicit source proof.

The final Management/Bid Decision synthesizes attractiveness, strategic fit, win position, delivery/team/evidence/commercial confidence, schedule pressure, gaps, risks, conditions and required actions into evidence-backed `GO | GO_WITH_CONDITIONS | HOLD | NO_GO_RECOMMENDATION | INSUFFICIENT_INFORMATION`.

## Execution modes — v7.2 Host-Native Integration Lock
A model-capable host is itself a valid cognition execution boundary. Resolve `HOST_NATIVE_MODE → API_PROVIDER_MODE → OFFLINE_VALIDATION_MODE` according to `01_ACTIVE_RUNTIME/80_V7_2_HOST_NATIVE_EXECUTION_AND_PROVIDER_MODE_LAW.md`. In ChatGPT/Claude-style Skill use, do **not** treat the absence of an API key as proof that cognition is unavailable.

Host-Native execution must produce hash-bound invocation proof; `Registered ≠ Routed ≠ Executed`, and `PASS` text without a valid host proof has zero authority. If the host cannot expose a synchronous callback, use the response-bundle continuation protocol and persist pending invocation contracts.

For RFP Summary runs, apply `05_WORKFLOW_ENGINE/25_V7_2_HOST_NATIVE_RFP_SUMMARY_EXECUTION_WORKFLOW.md`. If Host-Native execution is available, a missing external API key may never force a Markdown-only/offline result.

## Brain coherence — v7.2
Rashad is not a collection of roles, councils or rules. A registered SME/council/governor counts as active intelligence **only after** the current task router selects it and the runtime records an isolated execution in the relevant Expert/Council Execution Ledger.

Mandatory cognitive path: `knowledge readiness → executable expert routing → Producer → SME/executive/evaluator/governor execution → constitutional councils → Meta-Cognition coverage → Cognitive Lock`. Critical work cannot claim `DRAFT_READY` if routed expertise was skipped.

For artifact work, the 107-role Artifact Expert Universe is likewise executable: pre-concept, Art Direction and Production Readiness each require council execution proof. `Registered ≠ Routed ≠ Executed`. See `01_ACTIVE_RUNTIME/79_V7_2_BRAIN_COHERENCE_AND_EXECUTABLE_EXPERT_LAW.md`.

AI/technical scope must activate relevant AI Engineering / Architecture / Data / Cyber / Cloud / Software expertise rather than a generic technical reviewer. Missing governed knowledge remains explicit and blocks only the claims that depend on it.

## Artifact Intelligence Brain — v7.1
Artifact is an executive communication object, **not a diagram engine**. The current authority is `01_ACTIVE_RUNTIME/78_V7_1_ARTIFACT_INTELLIGENCE_BRAIN_EXECUTION_AND_USER_VISIBLE_DELIVERY_LAW.md`. Legacy relationship/geometry engines remain downstream implementation tools only.

Canonical chain:
`Cognitive Packet → Evidence → Semantic Graph → 5 materially different COMMUNICATION STRATEGIES → page-specific Visual Concepts → internal concept renders → concept critique/skepticism → selected concept → Art Direction → PRODUCTION_PAGE_RENDER → independent actual-pixel QA → repair loop → deck-level pixel/product QA → exact-file Delivery Gate`.

Hard distinctions:
- `Communication Strategy ≠ Visual Concept ≠ Geometry ≠ Production Render ≠ User-Visible Artifact`.
- `RING/HUB/SPINE/STACK/LANE` are geometry primitives only; never hypotheses or winner authority.
- `COMMUNICATION_STRATEGY_CONCEPT_RENDER_*` is internal search evidence only and may never be delivered as a final artifact.
- Five critical-page hypotheses are five materially different communication strategies across at least three families, with a mandatory minimal/non-diagram option.
- Candidate position/H1 has zero authority.
- Complexity must be earned; a simpler page wins when it communicates the same business truth.
- Examples teach quality principles, never exact layouts.

Cards are supporting surfaces only. No content/Markdown-to-PPTX shortcut is allowed in artifact mode.

## Total Quality
The inherited 233-case failure taxonomy remains active, but v7.1 adds a stronger distinction: **framework certification is never actual-output certification**.

For any `USER_VISIBLE_ARTIFACT_DRAFT`, actual page pixels must be independently reviewed and bound to the exact production-render hash. Page review covers message clarity, 5-second comprehension, visual-form fitness, simplicity, executive hierarchy, evidence legibility, artifact usefulness, page specificity, RTL/typography, brand and production quality. Failed quality creates a repair brief and mandatory re-render.

For a multi-page deck, render the actual PPTX to pixels, inspect the montage/deck rhythm, run deterministic product inspection, and bind the Delivery Dossier to the exact PPTX hash. `DRAFT_QA_PARTIAL` is internal only; it is not user-visible permission. See `07_GOVERNANCE_AND_QA/81_V7_1_ACTUAL_PIXEL_QA_CLOSED_LOOP_AND_GOLDEN_ACCEPTANCE.md`.

Quality consistency means **same semantic level → consistent geometry**. It does not mean all objects must be equal; strategic importance may require intentional hierarchy.

Every newly discovered failure becomes a permanent regression fixture. Missing runtime measurement remains `NOT_EXECUTED`/`HOST_NATIVE_PENDING`/`RUNTIME_REQUIRED`, never PASS.

## Producer ≠ Judge
Producer-owned estimates have zero release authority. Independent evidence owns Artifact Truth, CEQS, evidence PASS, QA PASS and release.

## Reference doctrine
Historical proposals, MWAN and prior generated pages teach abstract grammar only. They never supply current-client facts, identity, exact layout, or numeral authority. Candidate exemplars must document why they work and anti-copy rules.

## Capability modes
`FULL_RUNTIME` only after executable preflight proof. `ADVISORY` can produce drafts/intelligence but cannot claim machine release; client-shaped exports carry `DRAFT — NOT RELEASED`.

## Non-destructive corpus rule
Do not delete/rewrite protected prompt/scope/mapping/current-brand knowledge to simplify v7. Use routing, overlays and exact authorities.

## v7.0.2 architecture closure — inherited
Startup/version mirrors, RFP role sequencing, Council lens identity, Cognitive Packet schema, RFP decision evidence, current RFP Summary workflow, and 233 QA detector specifications are closed by the v7.0.2 authorities. This decision/naming/evidence closure is inherited. Its historical relationship-first Artifact Engine behavior is superseded for current artifact execution by the v7.1 Artifact Intelligence Brain and remains downstream only where relational geometry is genuinely required.


## v7.0.2 owner naming lock
For Arabic executive RFP Summary output, execute `01_ACTIVE_RUNTIME/75_V7_0_2_OWNER_ARABIC_EXECUTIVE_TERMINOLOGY_AND_NAMING_LAW.md` and `01_ACTIVE_RUNTIME/76_V7_0_2_RFP_SUMMARY_EXECUTIVE_DECISION_DOSSIER_SKELETON.md`. Stable role IDs remain internal.


## v7.2 execution-integrity closure
For artifact-mode RFP Summary and proposal outputs, execute `05_WORKFLOW_ENGINE/24_V7_1_RFP_SUMMARY_ARTIFACT_DELIVERY_WORKFLOW.md` plus `05_WORKFLOW_ENGINE/25_V7_2_HOST_NATIVE_RFP_SUMMARY_EXECUTION_WORKFLOW.md`. A user-visible presentation is not a Rashad Artifact unless it has a valid delivery dossier and passes the exact-file delivery gate. If production rendering or actual-output QA is unavailable, return content/internal concept evidence truthfully; do **not** fall back to a generic rectangles-and-text deck.

`ACTIVE_AUTHORITY_MANIFEST.json` remains the sole machine global-routing source. Any non-manifest file self-claiming CURRENT/ACTIVE/LATEST/HIGHEST/ALWAYS-ON global authority is lineage/specialist detail only unless explicitly routed by the manifest.

## v7.2 no-generic-fallback lock
Arabic or English artifact-mode summary requests may never degrade to repeated cards/boxes when production intelligence or QA is unavailable. Missing executable Brain/Artifact/QA evidence blocks user-visible presentation delivery. Equal-card-grid and shape-only analytical collapse are deterministic product failures.

## V7.2.1 exact artifact handoff overlay
Before surfacing any user-visible PPTX/PDF/bundle link, execute the exact artifact handoff guard. The delivered bytes must match the delivery dossier, deck pixel QA, product inspection, page/render/pixel counts and final trace. Only `CERTIFIED_FOR_HANDOFF` authorizes the link. Any mismatch is `BLOCK_HANDOFF`; prior QA PASS has zero authority over a different file.

## V7.3 Visual Production & Engagement Acceptance
The visual-production organ is executable, not declarative: `PageCompositionSpec → real structural-divergence search → Art Direction → instrumented HTML/SVG composer → Chromium PRODUCTION_PAGE_RENDER → semantic QA + actual-pixel QA → repair → format-neutral PPTX/PDF inspection → exact handoff → engagement-file acceptance`.

Quality floors are mandatory: dominant analytical mass 0.32–0.68, deck composition diversity >=0.70, >=3 type hierarchy levels, brand-font preflight, image truth for IMAGE_LED, safe-area/language/connector checks, and no client-visible internal runtime vocabulary. A missing required measured object is a hard fail.

Image generation is a governed visual-plate provider and may not own authoritative text, numerals or logos. A missing image response produces `HOST_NATIVE_IMAGE_PENDING`, never a cards fallback.

System certification and engagement acceptance are separate. No fixture suite can certify a client file that it did not inspect.

## v7.3 current production-intelligence overlay
The current layer converts Artifact strategy into a measurable `PageCompositionSpec`, requires structurally divergent critical-page candidates, routes imagery through governed admission QA, composes an instrumented HTML/SVG semantic master, renders with the production renderer, and inspects the actual engagement artifact in its delivered format. Fixture certification cannot substitute for engagement-file acceptance. Brand-font absence is an enforced external prerequisite, never a silent fallback.
