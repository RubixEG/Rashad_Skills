# Pixel-Truth Release Blockers

Hard blockers include: any text overlap; clipped glyph; owner badge outside anchor; wrong node/edge/label count; detached/reversed connector; unintended z-order occlusion; off-canvas object; debug leakage; missing font; unapproved transform/crop; card-only artifact where higher-order artifact required; cover unrelated to thesis; inconsistent PDF/PPTX master; required gate not instrumented. Scores cannot compensate a hard blocker.
