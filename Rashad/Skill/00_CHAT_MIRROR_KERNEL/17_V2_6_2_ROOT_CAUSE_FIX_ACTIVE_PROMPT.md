> **V6.2.2 SUPERSESSION BANNER — Legacy v2.6.2 recovery prompt only. Its ALWAYS-ON authority is revoked; current manifest/route owns execution. Preserve only non-conflicting diagnostic insight.**

# V2.6.2 Root-Cause Recovery — Active Execution Prompt

STATUS: LEGACY v2.6.2 RECOVERY DETAIL — ALWAYS-ON / GLOBAL AUTHORITY REVOKED

## Trigger
Apply this prompt whenever Rashad is asked to create an artifact product, especially an `RFP Summary`, or whenever a previous output shows placeholder cover imagery, generic-card/table downgrade, missing Artifact Engine evidence, weak visual hierarchy, or HTML/PDF parity drift.

## Root-cause principle
Do not assume the Artifact Engine has executed merely because Artifact Intelligence rules exist in the knowledge corpus. Execution must be proven in the current run.

The historical weak forward-test failed because the production path skipped required behavior: it substituted a placeholder cover visual, simplified relationship-heavy pages into generic cards/tables, and claimed completion without proving preservation of the approved artifact chain into the rendered output.

## Mandatory recovery sequence
1. Re-enter product routing and confirm the requested artifact product.
2. For an RFP Summary cover, execute `15_COVER_ART_DIRECTOR.md` and generate a new competition-specific hero as an isolated visual ingredient.
3. Execute `16_COBRAND_LOGO_DIRECTOR.md` before branded composition.
4. For every analytical page, execute `03_ARTIFACT_ENGINE/43_ARTIFACT_EXECUTION_PROOF_AND_NO_DOWNGRADE_GATE.md`.
5. Persist the complete page trace: question → thesis → evidence → relationship → Artifact Intent → semantic nodes/edges → archetype → Visual Blueprint → Geometry Handoff → Artifact Council → render QA.
6. Reject generic cards/tables when the information relationship requires topology, dependency, hierarchy, sequence, system, comparison, causality, prioritization or decision logic.
7. Rebuild any page with `artifact_strength_score < 7/10` before release.
8. Run rendered-output inspection and HTML/PDF/PPTX parity checks when those projections are produced.
9. Never treat successful file export as proof of artifact quality.
10. Never report `DONE` until the Product Completion Contract and Release Completion Gate pass.

## No-downgrade rule
If the available deterministic composer cannot preserve the approved topology, return the page to the Artifact stage or report production blocked. Do not silently replace a strong artifact with cards, a plain table, a generic diagram, or a generated full-slide image.

## Production honesty
A rendered file can exist and still fail Rashad. Release status depends on semantic fidelity, artifact strength, exact branding, physical RTL, co-brand geometry, and projection parity—not on file existence alone.

## V2.6.4 section-level visual parity extension
When a role or section expands across multiple analytical pages, do not ideate/render the pages independently. After analytical page contracts are complete, run `18_SECTION_VISUAL_BOARD_DIRECTOR.md` to create one Golden Section Board and freeze a Section Visual Charter. Native reconstruction must then satisfy `44_SECTION_GOLDEN_BOARD_AND_PARITY_BRIDGE.md`, `45_VISUAL_FIDELITY_CONTRACT.md`, and the blocking Visual Parity Gate. The generated board remains reference-only; production facts/text/logos/numbers stay native.
