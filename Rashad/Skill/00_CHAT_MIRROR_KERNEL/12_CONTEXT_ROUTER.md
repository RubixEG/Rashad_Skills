# V7.2 Context Router / Compiler

**STATUS: CURRENT SELECTIVE-LOADING AUTHORITY**

## Authority precheck
Before any retrieval, validate root `AUTHORITY_BINDING_CHECK.json`, then apply root `ACTIVE_AUTHORITY_MANIFEST.json` and `RETRIEVAL_EXCLUSION_REGISTRY.json`. A mismatch is `VERSION_CONFLICT_BLOCK`.

## Always-on kernel
Load only:
- current Bootstrap;
- Owner Policy;
- current V7.2 Authority Graph;
- Version Layer/Retirement Ledger;
- `53_V6_2_ACTIVE_AUTHORITY_REGISTRY.md`;
- root `ACTIVE_AUTHORITY_MANIFEST.json`;
- root `RETRIEVAL_EXCLUSION_REGISTRY.json`;
- Product Registry;
- current owner decisions only when mirrored into manifest-listed authorities; historical Decision Ledger is memory/provenance, not global routing;
- No-Unnecessary-Clarification policy;
- V6.1 Execution-Proof Director;
- v6.2.2 Capability Mode contract;
- document-instruction isolation firewall;
- page criticality classification contract.

## Routing procedure
1. Identify/reset the current engagement.
2. Classify the requested product.
3. Inventory current RFP/evidence relevant to the product.
4. Apply retrieval exclusions BEFORE semantic/keyword retrieval.
5. Resolve authorities by exact path; numeric prefixes alone are forbidden identifiers.
6. Load canonical product/skeleton authorities.
7. Resolve exact R-code plan and retrieve only required exact prompt shards.
8. For analytical pages, first resolve `HOST_NATIVE_MODE | API_PROVIDER_MODE | OFFLINE_VALIDATION_MODE`; a model-capable host uses Host-Native execution even if the Python environment lacks an API key. Then derive fail-closed criticality, compile the Cognitive Packet, execute the inherited v7.2 Expert Execution Ledger, then load the v7.3 PageCompositionSpec / production-organ authorities plus the inherited V7.1 Artifact Intelligence Brain + v7.2 coherence/execution-proof contracts. Legacy V6 relationship/geometry modules are downstream specialist tools only when the chosen communication strategy requires them.
9. Execute only the routed general SMEs/executive/evaluator/governor roles and persist the Expert Execution Ledger; registered-but-unexecuted roles count as missing, not as coverage.
10. Load task-relevant councils, brand, RTL/BiDi, appendix/evidence, and production modules. For Saudi-government analytical products, path-load `07_GOVERNANCE_AND_QA/05_MINISTRY_ACCEPTANCE_MODEL.md`, `07_GOVERNANCE_AND_QA/06_AWARDABILITY_SCORE.md`, and `03_ARTIFACT_ENGINE/55_MINISTRY_GRADE_PAGE_SCORING_MODEL.md` when applicable.
11. Apply only owner decisions that are mirrored into current manifest authorities. The historical owner decision ledger never overrides the manifest.
12. Exclude historical/superseded/rejected route bodies even if their text says CURRENT/HIGHEST.
13. Apply no-clarification gate before asking the owner.

## Hard exclusions from bulk context
- `02_IMMUTABLE_AUTHORITIES/Rashad_Prompts_Master_Document.md` must never be bulk-loaded; retrieve exact shards from `RETRIEVAL/PROMPTS/`.
- all 388 prompts at once;
- all historical proposals;
- all service-line playbooks unless product scope requires them;
- any path listed in `RETRIEVAL_EXCLUSION_REGISTRY.json` as `GLOBAL_ROUTE_EXCLUDED`.

## Critical analytical page invariant
Analytical client-facing pages default critical; producer self-downgrade is forbidden.
Question → thesis → evidence → semantic graph → 5 communication strategies → page-specific visual concepts → internal concept renders → production render → independent actual-pixel QA → repair → deck QA → exact-file delivery gate.

## Anti-bloat / anti-lineage rule
Never load multiple retired production lineages merely because they exist. Legacy specialist knowledge may be used only after the current registry explicitly permits it and only for non-conflicting detail.


## V7 conditional council routing
For RFP Summary, route canonical role registry + current v7.3 composition/production and engagement-acceptance authorities + inherited v7.2 Brain-coherence + Host-Native execution authorities + inherited v7 decision workflow and v7.1 artifact-delivery foundation + monolingual naming + role-specific cognitive packet + the lens-to-authorized-role registry + only the relevant Council-of-Councils lenses + Role-24 decision evidence contract. Do not load the entire executive/government roster on every page. For visual/QA work, retrieve the failure taxonomy category and artifact-family QA relevant to the page rather than bulk-loading all QA prose.

## V7.2.1 exact artifact handoff overlay
Before surfacing any user-visible PPTX/PDF/bundle link, execute the exact artifact handoff guard. The delivered bytes must match the delivery dossier, deck pixel QA, product inspection, page/render/pixel counts and final trace. Only `CERTIFIED_FOR_HANDOFF` authorizes the link. Any mismatch is `BLOCK_HANDOFF`; prior QA PASS has zero authority over a different file.
