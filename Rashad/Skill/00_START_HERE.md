# Rashad v7.3.0 — Start Here

**STATUS: CURRENT SKILL STARTUP ROUTE**  
**BASELINE: v7.0.2 decision/naming/evidence architecture inherited; v6.2.2 protected corpus preserved.**  
**RUNTIME NOTE: Rashad OS Brain v3.5, Artifact Brain v4.0 and QA v4.4 / QA Brain v1.4 implement the user-visible delivery boundary. In a model-capable host, use `HOST_NATIVE_MODE`; absence of an external API key does not imply Offline mode. Internal concept search is not production output. Production `RELEASED` still requires externally independent release evidence and final parity proof.**

There is one global Skill route:
`Bootstrap → Owner Policy → Version Ledger → ACTIVE_AUTHORITY_MANIFEST → Retrieval Exclusions → Product Registry → Context Router → protected execution/firewall authorities → inherited v7.0.2 decision/naming/evidence authorities → inherited v7.1 Artifact Intelligence + Actual-Pixel QA + Delivery Integrity authorities → inherited v7.2 Brain Coherence + Executable Expertise authorities → v7.3 Composition/Production + Engagement Acceptance authorities → task-local specialist authorities`.

## Artifact-mode page stages
1. `CONTENT / EVIDENCE LOCK`
2. `COGNITIVE PACKET LOCK`
3. `SEMANTIC GRAPH LOCK`
4. `FIVE COMMUNICATION STRATEGIES` — exactly 5; ≥3 families; minimal/non-diagram candidate required
5. `INTERNAL CONCEPT SEARCH` — concept renders are never user-visible
6. `CONCEPT CRITIQUE + ARTIFACT SKEPTIC`
7. `VISUAL CONCEPT + ART DIRECTION LOCK`
8. `PRODUCTION_PAGE_RENDER`
9. `INDEPENDENT ACTUAL-PIXEL QA` — exact render hash
10. `REPAIR CLOSURE`
11. `DECK / PRODUCT QA` — actual PPTX inspection + montage/rhythm review
12. `EXACT-FILE DELIVERY GATE`
13. `RELEASE CANDIDATE / RELEASE` only when additional independent/parity proof exists

Before Stage 2 cognition, resolve execution mode: `HOST_NATIVE_MODE | API_PROVIDER_MODE | OFFLINE_VALIDATION_MODE`. Host-capable Skill sessions must route through the Host-Native bridge rather than silently falling to Offline because a Python API key is absent.

Skipping a required stage is a hard block. Candidate position has zero authority. `DRAFT_QA_PARTIAL` and framework certification cannot satisfy user-visible output gates.

## RFP Summary startup
Load the canonical 24-role registry/order, v7.0.2 owner naming authority, current v7.3 execution route: decision workflow 23 + inherited v7.1 artifact-delivery workflow 24 + Host-Native workflow 25 + v7.3 composition/production workflow 26 + inherited v7.2 Brain Coherence gates, Council lens registry/router, Consulting Cognitive Packet contract, RFP ingestion/execution schemas and Decision Evidence contract. The 15-step decision pipeline is machine-gated by `Rashad/Brain/runtime/rfp_summary_runtime.py`; artifact-mode steps additionally execute `05_WORKFLOW_ENGINE/24_V7_1_RFP_SUMMARY_ARTIFACT_DELIVERY_WORKFLOW.md`, production rendering, actual-output QA and exact-file delivery.

## Retrieval safety
Numeric filename prefixes are ordering hints, not authority IDs. Apply `RETRIEVAL_EXCLUSION_REGISTRY.json` before keyword/semantic retrieval. `ACTIVE_AUTHORITY_MANIFEST.json` is the sole machine global-routing authority. Historical/current-looking bodies are non-governing unless explicitly routed by the manifest for non-conflicting specialist detail.

## V7.2.1 exact artifact handoff overlay
Before surfacing any user-visible PPTX/PDF/bundle link, execute the exact artifact handoff guard. The delivered bytes must match the delivery dossier, deck pixel QA, product inspection, page/render/pixel counts and final trace. Only `CERTIFIED_FOR_HANDOFF` authorizes the link. Any mismatch is `BLOCK_HANDOFF`; prior QA PASS has zero authority over a different file.
