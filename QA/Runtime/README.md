# Rashad Unified QA Runtime v4.3 — Actual Product Inspection & Closed-Loop Delivery Lock

This is the canonical deterministic QA runtime inside `Rashad_OS/QA`. It is bound to Rashad Proposal OS v7.2.0 and is paired with the independent cognitive QA layer at `../Brain`.

## Authority boundary
- Deterministic QA measures and produces proof.
- Independent QA Brain challenges the proof using separate council actors/contexts.
- QA can produce **QA_CANDIDATE_PASS** only.
- QA can never issue production **RELEASED**.
- Production `RELEASED` is owned only by the **Rashad Brain Release Chair**.

## Current executable coverage
- 233 failure-taxonomy handler contracts.
- 21 artifact-family QA contracts.
- 20 stress/chaos mutations.
- HTML/DOM/pixel/geometry/RTL/BiDi/brand/evidence/cross-deck checks.
- Render-grounded independent artifact/visual judge contract validation.
- Proof-integrity, stale-hash, source locator, parity and Skill-binding gates.
- Arabic executive terminology gate inherited from v7.0.2 and bound under the current v7.2.0 route.
- Brain-aware execution dossier validation.

## Canonical Skill location
By default regression scripts resolve the sibling final package path: `../../Rashad/Skill`. Override with `RASHAD_SKILL_ROOT` when embedding elsewhere.

## Important truthfulness rule
This package certifies the QA framework itself. It does **not** pre-certify future proposals. Every actual Rashad product must provide current render/evidence/judge proof and pass the full runtime.
