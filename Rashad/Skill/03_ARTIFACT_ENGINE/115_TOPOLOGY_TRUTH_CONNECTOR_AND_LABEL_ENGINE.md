# Topology Truth, Connector & Label Engine

Higher-order artifact truth requires geometry, not a `data-artifact-type` label. Validate expected node/edge IDs and counts; source/target ownership; rendered endpoints; arrowhead owner; branch/cycle direction; route clipping; label attachment; edge-node collisions; edge-label collisions; and post-RTL direction. Fake SYSTEM/NETWORK/TIMELINE wrappers around card grids fail.
