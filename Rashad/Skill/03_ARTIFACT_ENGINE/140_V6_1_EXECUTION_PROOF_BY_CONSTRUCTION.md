# V6.1 Execution Proof by Construction

## Problem closed
Previous runs could export pages after a lightweight rendering path while merely naming the V6 engines in prose. V6.1 makes the intermediate reasoning/design products first-class release artifacts.

## Rule
Every downstream asset must reference hashes of its upstream evidence. A final page master cannot be admitted if its dossier lacks the required chain.

## Staleness
If content, graph, selected exhibit, evidence pack, visual-search winner or master changes, all dependent downstream PASS states are invalid until rerun.

## Release algebra
`PageRelease = Content × Graph × ArtifactTruth × ExhibitSearch × VisualSearch × CEQS × RenderQA × Stress/RepairSafety × MasterIntegrity`

Any zero/blocking term makes PageRelease zero.
`ProductRelease = ALL(PageRelease) × ExportParity × Firewall × Compliance`.
