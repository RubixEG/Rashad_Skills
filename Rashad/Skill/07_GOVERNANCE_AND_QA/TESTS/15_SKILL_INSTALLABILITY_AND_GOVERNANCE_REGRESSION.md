# V2.2 Installability & Governance Regression Test

Required checks:
1. Root `SKILL.md` starts with valid YAML frontmatter containing non-empty `name` and `description`.
2. One canonical node-state vocabulary only appears in active state definitions.
3. Main E2E and Proposal SOP activate parallel workstreams after Bid Strategy; no reader-number serialization rule remains.
4. Engagement State includes Council Session, Council Finding, Approval, Dependency, Compliance and Reverse Bid Calendar ledgers.
5. Exact retrieval sharding count = 388 prompts + 96 scopes + 96 mappings; IDs unique.
6. Arabic prose numeral authority = Arabic-Indic; no active source-following Western-digit instruction remains.
7. Production hero = strictly text/logo/number-free.
8. Near-black is mathematically defined.
9. Logo validation uses provenance + geometry, not post-resize pixel equality.
10. Runtime execution claims remain `NOT EXECUTED` unless an actual external renderer/test runner ran.
