MODULE: 06_RFP_SUMMARY
STATUS: AUTHORITATIVE — vNext

Load and execute `22_RFP_SUMMARY_FINAL_PRODUCT_CONTRACT.md`. The default meaning of “RFP Summary” is `INTERNAL_PURSUIT_BRIEF` unless the user explicitly requests a client-facing understanding artifact.

Core invariant: dynamic logical roles, not fixed slide count. Cover hero image is generated per engagement; production text and logos remain native/editable.

All risk and clarification logic is delegated to `21_RISK_AND_CLARIFICATION_COUNCIL.md`.

Rendering is artifact-first: Question → Thesis → Evidence → Relationship → Artifact Intent → Brand → Geometry → QA.

Required v2.4 gates: execute `32_RFP_AUTHORSHIP_AND_PROCUREMENT_MATURITY.md` for the internal product and `33_ARABIC_VISIBLE_LANGUAGE_PURITY_GATE.md` for Arabic visible output.
