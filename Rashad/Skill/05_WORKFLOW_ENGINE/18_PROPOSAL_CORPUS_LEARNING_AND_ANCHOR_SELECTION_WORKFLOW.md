
# Proposal Corpus Learning & Anchor Selection Workflow

`Corpus inventory → proposal-type classification → page-family sampling → current-quality scoring → extract grammar → reject legacy weaknesses → create style/structure anchors → current-engagement translation`.

Never feed an entire old proposal as a template. Select only relevant family anchors and remove old client facts/branding from authority.
