# Executable QA Harness Companion Interface

STATUS: RUNTIME COMPANION CONTRACT — v2.6.4.6 / QA Harness v1.2

The separately supplied `rashad-qa-harness-v1.2-production-integration` is an executable QA companion, not part of the portable Markdown Skill.

## Browser/page gates
G01–G14 execute overflow, overlap, containment, structural RTL, numerals, resolved fonts, brand hash, co-brand optical geometry, luminance, governed topology lineage, palette, Golden Master provenance/canvas and transform-integrity checks.

## Integration additions
- Canonical Page Spec → QA Spec compiler;
- page-specific `N_A` applicability;
- GVM semantic topology sidecar rather than fake DOM nodes;
- mandatory master SHA for GVM pages;
- master/logo crop/mirror/filter/stretch protection;
- 2% co-brand visible-height tolerance and 5% alpha threshold;
- current Rubix font profile synchronization;
- structured runtime-block evidence;
- `HTML_PREEXPORT_PASS` instead of HTML `RELEASED`;
- PDF/PPTX raster parity commands;
- deck continuity command;
- final evidence aggregator.

## Boundary
The harness enforces measurable production conditions. It does not make consulting decisions, choose artifacts, change the owner-locked proposal skeleton, or replace Consulting Visual/Artifact Councils.
