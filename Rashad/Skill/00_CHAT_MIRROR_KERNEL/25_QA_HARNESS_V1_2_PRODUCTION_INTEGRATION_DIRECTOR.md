# QA Harness v1.2 Production Integration Director

STATUS: ACTIVE — v2.6.4.6

## Mission
Connect approved Rashad page intelligence and Golden Visual Masters to the external executable QA companion without allowing HTML pre-export checks to masquerade as final PDF/PPTX release.

## Required chain
`Canonical Page Spec → QA Spec Compiler → semantic topology sidecar → Golden Master + native overlays → HTML projection → QA Harness v1.2 → HTML_PREEXPORT_PASS → PDF/PPTX export → raster parity QA → Deck Continuity QA → Final Evidence Aggregator → Release Council`.

## Hard distinctions
- Skill = intelligence/governance.
- Harness = executable enforcement companion.
- `HTML_PREEXPORT_PASS` ≠ `RELEASED`.
- `RELEASED` requires HTML pre-export, PDF parity, PPTX parity and deck continuity evidence, plus applicable consulting/release councils.

## Golden Master topology
Do not add fake invisible DOM nodes merely to satisfy topology QA. For full-page Golden Visual Masters, G11 uses a governed semantic topology sidecar compiled from the locked Page Spec. The sidecar proves semantic lineage; it does not claim computer vision has reconstructed the raster.

## Gate applicability
Non-applicable checks use `N_A`. Required but unavailable checks use `NOT_EXECUTED`/`BLOCKED`. PASS requires execution evidence.

## Current runtime profile
- co-brand visible-height tolerance: `0.02`;
- alpha visible-mask threshold: `0.05`;
- approved production font families: `Montserrat`, `Montserrat Arabic` unless an evidenced current engagement override exists;
- GVM master SHA required for GVM pages;
- governed master/logos may not be mirrored, filtered, clipped, cropped, or non-uniformly stretched.
