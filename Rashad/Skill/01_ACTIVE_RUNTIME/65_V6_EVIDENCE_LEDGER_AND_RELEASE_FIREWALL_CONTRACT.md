# 65 — V6 Evidence Ledger & Release Firewall Contract

Every factual source used in a page must resolve to `schemas/evidence-ledger.schema.json`. `REFERENCE_ONLY` sources may inform grammar or methodology but may not prove current-engagement facts.

Final release also requires a persisted `execution-firewall` object. A prose assertion that a flag is true is insufficient; the release runtime consumes the machine object. Missing or false flag = BLOCKED.
