# Layout, Padding, Alignment, Spacing & Scale Engine

Measure actual padding, gaps, alignment lines, grid tracks and item sizes. Detect collapsed/negative gaps, inconsistent group spacing, unexpected flex shrink/grow, min/max-size violations, excessive element scaling, undersized/oversized cards, and content touching container edges. Reflow must remain coherent when content grows.
